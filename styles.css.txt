:root{
  --bg:#071021; --card:#0e1a2b; --accent:#4fd1c5; --accent-2:#7c6cff;
  --muted:#9aa4b2; --glass: rgba(255,255,255,0.03); --container:1100px;
  --glass-2: rgba(255,255,255,0.04);
}
*{box-sizing:border-box}
html,body{height:100%}
body{
  margin:0;font-family:Inter,system-ui,-apple-system,"Segoe UI",Roboto;
  background:linear-gradient(180deg,#040617 0%,#071021 60%);
  color:#eaf6f5; -webkit-font-smoothing:antialiased;
}

/* Layout container */
.container{max-width:var(--container);margin:0 auto;padding:0 18px}

/* Header */
.site-header{position:fixed;left:0;right:0;top:0;padding:12px 0;z-index:60;background:linear-gradient(180deg, rgba(2,6,23,0.45), rgba(2,6,23,0.15));backdrop-filter: blur(6px)}
.site-header .container{display:flex;align-items:center;justify-content:space-between}
.logo{font-family:'Press Start 2P',cursive;color:var(--accent);text-decoration:none;font-size:0.95rem}
.nav a{color:rgba(230,245,244,0.9);margin-right:14px;text-decoration:none;font-weight:600}
.btn{background:linear-gradient(90deg,var(--accent-2),var(--accent));color:#021018;padding:10px 14px;border-radius:10px;border:none;cursor:pointer;font-weight:700;box-shadow:0 8px 30px rgba(115,95,255,0.12)}
.btn.ghost{background:transparent;border:1px solid rgba(255,255,255,0.06);color:#dff7f2}
.btn.small{padding:6px 10px;font-size:0.9rem}
.btn.large{padding:12px 18px;font-size:1rem}
a.btn{text-decoration:none;display:inline-block}

/* HERO */
.hero{position:relative;min-height:72vh;padding-top:78px;overflow:hidden}
.bg-base{
  position:absolute;inset:0;background-image:url('images/A_vibrant_digital_illustration_serves_as_the_homep.png');
  background-size:cover;background-position:center;filter:contrast(1.05) saturate(1.05) brightness(0.95);z-index:1;
  transform-origin:center;
  /* slow zoom */
  animation: bgZoom 20s linear infinite;
  will-change:transform;
}
@keyframes bgZoom{0%{transform:scale(1)}50%{transform:scale(1.02)}100%{transform:scale(1)}}

/* neon sweep (overlay) */
.bg-base::after{
  content:"";position:absolute;inset:0;
  background:linear-gradient(90deg, rgba(124,108,255,0.06), rgba(79,209,197,0.06), rgba(124,108,255,0.02));
  mix-blend-mode:screen;pointer-events:none;z-index:2;
  animation: sweep 8s linear infinite;
}
@keyframes sweep{0%{background-position:-200% 0}50%{background-position:200% 0}100%{background-position:-200% 0}}

/* floating game images (absolute) */
.game-img{position:absolute;z-index:6;transform-origin:center;filter:drop-shadow(0 12px 30px rgba(2,6,23,0.6));border-radius:12px;overflow:hidden}
.game-img img{display:block;width:100%;height:100%;object-fit:cover}

/* positions & sizes (desktop) */
.g-1{right:6%;top:10%;width:320px;height:200px;animation: float1 6s ease-in-out infinite}
.g-2{left:6%;top:18%;width:220px;height:150px;animation: float2 7s ease-in-out infinite}
.g-3{right:8%;bottom:8%;width:260px;height:160px;animation: float3 5.5s ease-in-out infinite}
.g-4{left:12%;bottom:12%;width:200px;height:140px;animation: float4 6.2s ease-in-out infinite}

/* subtle float animations */
@keyframes float1{0%{transform:translateY(0) rotate(-2deg)}50%{transform:translateY(-8px) rotate(2deg)}100%{transform:translateY(0) rotate(-2deg)}}
@keyframes float2{0%{transform:translateY(0) rotate(1deg)}50%{transform:translateY(-6px) rotate(-1deg)}100%{transform:translateY(0) rotate(1deg)}}
@keyframes float3{0%{transform:translateY(0) rotate(-1deg)}50%{transform:translateY(-7px) rotate(1deg)}100%{transform:translateY(0) rotate(-1deg)}}
@keyframes float4{0%{transform:translateY(0) rotate(2deg)}50%{transform:translateY(-5px) rotate(-2deg)}100%{transform:translateY(0) rotate(2deg)}}

/* hero inner content */
.hero-inner{position:relative;z-index:8;display:flex;align-items:center;justify-content:space-between;padding:48px 0}
.hero-text{max-width:560px}
.hero-text h1{font-size:2rem;margin:0;color:var(--accent-2);text-shadow:0 6px 30px rgba(124,108,255,0.16)}
.subtitle{color:rgba(230,245,244,0.9);margin:8px 0 18px}
.hero-preview img{width:420px;border-radius:14px;box-shadow:0 20px 60px rgba(3,9,26,0.6);border:1px solid rgba(255,255,255,0.04)}

/* content */
main{padding:28px 0 60px}
.products h2{color:var(--accent);margin-bottom:12px}
.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:18px}
.card{background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));padding:14px;border-radius:12px;box-shadow:0 8px 30px rgba(2,6,23,0.6);transition:transform .18s}
.card:hover{transform:translateY(-6px)}
.card img{width:100%;height:150px;object-fit:cover;border-radius:8px;margin-bottom:10px}
.card h3{margin:0 0 6px;color:var(--accent-2)}
.meta{display:flex;justify-content:space-between;align-items:center}

/* contact */
.contact{margin-top:26px;padding:20px;background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));border-radius:12px}
.contact h2{color:var(--accent);margin-bottom:12px}
.contact-form label{display:block;margin:8px 0;color:#eaf6f5}
input,textarea{width:100%;padding:9px;border-radius:8px;border:none;background:rgba(0,0,0,0.35);color:#eaf6f5;outline:none}
.contact .btn{margin-top:8px}

/* footer */
.site-footer{padding:18px 0;text-align:center;color:var(--muted);border-top:1px solid rgba(255,255,255,0.03);margin-top:30px}

/* modal */
.modal{position:fixed;inset:0;display:flex;align-items:center;justify-content:center;background:rgba(2,6,23,0.7);z-index:200}
.hidden{display:none}
.modal-content{background:linear-gradient(180deg,#071026,#06101a);padding:20px;border-radius:12px;width:360px;box-shadow:0 20px 60px rgba(2,6,23,0.7);position:relative}
.modal-close{position:absolute;right:10px;top:10px;background:transparent;border:none;color:#fff;font-size:18px;cursor:pointer}

/* chatbot */
#chatbot{position:fixed;right:18px;bottom:18px;width:320px;border-radius:12px;overflow:hidden;box-shadow:0 10px 36px rgba(2,6,23,0.5);z-index:120}
.chatbot-closed{transform:translateY(90%);transition:transform .28s}
#chat-header{background:linear-gradient(90deg,var(--accent-2),var(--accent));padding:10px;color:#021018;font-weight:700;cursor:pointer}
#chat-body{height:200px;overflow-y:auto;padding:12px;background:linear-gradient(180deg,#081426,#061028);color:#eaf6f5}
#chat-input-container{display:flex;border-top:1px solid rgba(255,255,255,0.03)}
#chat-input{flex:1;padding:10px;border:none;background:transparent;color:#eaf6f5}
#chat-send{padding:10px;border:none;background:var(--accent);cursor:pointer;color:#021018}

/* responsive */
@media(max-width:900px){
  .hero-inner{flex-direction:column-reverse;padding:28px 0}
  .hero-preview img{width:88%;max-width:360px;margin-bottom:18px}
  .g-1{right:4%;top:6%;width:220px;height:140px}
  .g-2{left:4%;top:12%;width:160px;height:110px}
  .g-3{right:6%;bottom:6%;width:200px;height:120px}
  .g-4{left:8%;bottom:8%;width:160px;height:110px}
  #chatbot{right:12px;bottom:12px;width:280px}
}

/* small phones: reduce heavy visuals */
@media(max-width:480px){
  .bg-base{background-position:center 35%}
  .hero-text h1{font-size:1.2rem}
  .hero-preview{display:none}
  .game-img{display:none}
  .container{padding:0 12px}
}
